[package]
name = "xframe-core"
version = "0.0.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
ahash = "0.8"
wasm-bindgen = "0.2"

[dependencies.xframe-reactive]
path = "../reactive"
